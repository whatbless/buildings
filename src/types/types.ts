export type questionType = {
  title: string;
};

export type propertyTypeVariants = {
  var: string;
};

export type carouselObject = {
  src: string;
  title: string;
  desc: string;
  text: string;
};
